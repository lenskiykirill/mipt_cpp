#include <iostream>

namespace dst {
	class BigInteger {
		public:
			BigInteger ();
			BigInteger (int InitValue);
			BigInteger (unsigned int InitValue);
			BigInteger (long long IntValue);
			BigInteger (unsigned long long InitValue);

		private:
			unsigned long long 
	};

	BigInteger::BigInteger () {
		return;
	}

	BigInteger::BigInteger (int InitValue) {
		return;
	}

	BigInteger::BigInteger (unsigned int InitValue) {
		return;
	}

	BigInteger::BigInteger (long long InitValue) {
		return;
	}

	BigInteger::BigInteger (unsigned long long InitValue) {
		return;
	}
}

int main () {
	dst::BigInteger a (-5);
	return 0;
}
